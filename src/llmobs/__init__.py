"""llmobs — lightweight observability + tracing for LLM apps.

Instrument any LLM call to capture prompt, model, tokens, latency, cost, and a quality
score; persist spans to a local store; run online evals on sampled traffic; and explore
every trace in a dashboard.
"""

__version__ = "0.1.0"
